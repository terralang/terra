name: CI
on: [push]

jobs:
  build:
    name: Build Ubuntu 16.04
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - run: ./travis.sh
        env:
          DOCKER_BUILD: ubuntu-16.04
